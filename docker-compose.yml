services:
  plantuml-server:
    build:
      context: .
      dockerfile: Dockerfile.jetty
    image: plantuml/plantuml-server:local
    container_name: plantuml-server
    ports:
      - 5500:8080
    environment:
      - BASE_URL=plantuml-viewer
